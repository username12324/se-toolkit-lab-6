# Task 1 plan

Implement a minimal CLI agent that:

- reads `LLM_API_KEY`, `LLM_API_BASE`, and `LLM_MODEL` from env;
- accepts a question from CLI;
- calls an OpenAI-compatible LLM API;
- prints JSON output.
